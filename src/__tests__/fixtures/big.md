# bench fixture

## section 0: selection token

Syntax measure viewport match highlight cursor unicode flex opentui conceal scroll scroll viewport watch reflow syntax selection markdown stream measure render cursor theme measure flex mermaid flex highlight shiki sidebar yank viewport sidebar shiki sidebar layout flex render theme buffer selection lexer measure search render scroll shiki opentui render.

> Buffer layout syntax selection layout conceal theme unicode emoji token sidebar highlight.

Reflow emoji match reflow parser sidebar conceal markdown markdown token shiki clipboard measure cursor viewport highlight opentui parser search conceal reflow selection.

## section 1: measure reflow

Measure parser cursor parser buffer clipboard unicode search measure yank unicode viewport buffer highlight flex flex flex stream render highlight syntax yank markdown parser yank opentui measure clipboard cursor sidebar search lexer markdown yank selection diagram parser scroll measure parser theme markdown layout lexer render token watch emoji conceal.

### yank match

- Match yank render shiki measure syntax parser reflow.
- Opentui cursor layout reflow unicode.
- Scroll measure stream buffer emoji.
- Shiki opentui stream selection emoji selection conceal lexer search scroll.
- Buffer diagram token shiki cursor search flex diagram parser layout.
- Cursor diagram unicode token render parser token reflow search.
- Reflow render shiki mermaid stream measure mermaid opentui.

Watch viewport theme parser render parser unicode scroll conceal parser conceal sidebar mermaid conceal token render syntax flex cursor measure render.

## section 2: viewport mermaid

Viewport viewport clipboard cursor opentui lexer measure diagram token token opentui markdown stream markdown yank cursor conceal sidebar shiki buffer search syntax token diagram syntax theme match highlight stream selection scroll clipboard syntax token cursor viewport emoji layout sidebar yank viewport yank mermaid measure shiki emoji diagram syntax yank highlight layout markdown shiki watch.

| col a | col b | col c |
|-------|-------|-------|
| match | clipboard0 | 0 |
| mermaid | selection1 | 7 |
| diagram | highlight2 | 14 |
| highlight | selection3 | 21 |

Diagram stream search opentui flex viewport layout token watch match measure parser cursor render watch emoji diagram unicode clipboard shiki scroll buffer highlight token buffer search match selection selection render render parser parser measure reflow viewport unicode cursor cursor mermaid mermaid mermaid.

## section 3: layout viewport

Lexer highlight flex syntax search highlight viewport lexer token render render markdown syntax sidebar sidebar highlight match match viewport token flex highlight stream selection markdown yank match reflow scroll layout.

### buffer clipboard

- Watch match token stream layout.
- Sidebar reflow search unicode sidebar search watch theme token buffer.
- Sidebar stream mermaid token selection reflow highlight mermaid unicode.
- Token search syntax selection match reflow opentui render.
- Clipboard clipboard selection buffer clipboard lexer reflow theme stream lexer.

> Viewport watch emoji shiki stream yank buffer mermaid unicode selection layout scroll.

Syntax render measure mermaid scroll syntax shiki yank lexer shiki unicode flex shiki watch opentui highlight reflow stream sidebar emoji lexer diagram clipboard parser opentui buffer scroll yank unicode markdown.

## section 4: cursor unicode

Emoji buffer scroll viewport stream buffer conceal theme search diagram opentui sidebar buffer reflow stream selection emoji viewport measure flex highlight stream diagram emoji emoji highlight match reflow flex watch mermaid match search conceal clipboard search lexer cursor layout opentui scroll cursor selection conceal stream syntax sidebar clipboard mermaid parser scroll buffer syntax.

```typescript
const theme0 = syntax(0);
const stream1 = syntax(1);
const yank2 = clipboard(2);
const layout3 = opentui(3);
```

> Conceal scroll parser flex emoji match stream opentui theme shiki clipboard scroll.

Cursor token watch mermaid shiki selection theme buffer viewport selection watch syntax match render theme markdown yank render selection conceal selection watch reflow highlight.

## section 5: diagram emoji

Parser scroll layout conceal markdown selection scroll token clipboard reflow syntax reflow selection conceal watch watch layout mermaid conceal emoji viewport selection match theme conceal search yank sidebar sidebar watch clipboard viewport watch render search search flex parser mermaid search mermaid markdown theme conceal render.

```rust
fn reflow_0() -> cursor { 0 }
fn conceal_1() -> measure { 1 }
fn buffer_2() -> clipboard { 2 }
fn render_3() -> buffer { 3 }
```

> Stream match viewport theme conceal syntax scroll search highlight scroll flex lexer.

| col a | col b | col c |
|-------|-------|-------|
| mermaid | syntax0 | 0 |
| emoji | sidebar1 | 7 |
| buffer | render2 | 14 |
| stream | measure3 | 21 |
| scroll | token4 | 28 |

Sidebar yank token emoji shiki layout reflow reflow flex reflow syntax sidebar scroll unicode lexer theme conceal render cursor highlight token flex measure theme viewport measure selection reflow measure search unicode viewport.

## section 6: cursor match

Clipboard lexer conceal highlight diagram watch stream scroll theme emoji match search cursor syntax conceal token cursor measure token reflow watch measure reflow selection opentui yank match scroll mermaid clipboard cursor shiki clipboard opentui shiki theme viewport viewport markdown lexer token selection buffer syntax stream flex opentui.

### buffer search

- Viewport unicode lexer sidebar clipboard selection stream highlight reflow.
- Selection selection cursor watch sidebar watch yank stream flex highlight.
- Opentui cursor search mermaid parser mermaid conceal.
- Search viewport token emoji buffer emoji.

| col a | col b | col c |
|-------|-------|-------|
| render | conceal0 | 0 |
| lexer | search1 | 7 |
| scroll | shiki2 | 14 |

Flex conceal yank search selection render syntax yank render measure search scroll match measure reflow parser markdown unicode highlight sidebar search flex clipboard selection yank syntax layout watch.

## section 7: match selection

Parser shiki shiki parser token layout layout stream scroll scroll measure mermaid reflow search search search render layout highlight shiki viewport emoji mermaid match yank parser search selection measure opentui search match diagram match highlight selection match clipboard yank stream markdown emoji lexer measure conceal clipboard opentui buffer.

### token scroll

- Measure stream mermaid yank token.
- Match opentui buffer cursor selection highlight emoji markdown diagram.
- Match highlight buffer measure stream emoji mermaid token.
- Viewport shiki cursor syntax lexer shiki markdown.
- Scroll theme mermaid search watch unicode unicode markdown syntax lexer.
- Scroll match yank watch sidebar shiki render viewport.
- Opentui scroll sidebar yank conceal measure.

```bash
conceal --lexer "markdown"
reflow --lexer "theme"
measure --measure "emoji"
emoji --watch "unicode"
theme --yank "watch"
```

Emoji diagram render match lexer render lexer cursor scroll viewport markdown reflow token selection watch measure measure search reflow sidebar emoji syntax scroll emoji sidebar measure lexer measure yank selection diagram layout emoji selection conceal.

## section 8: clipboard stream

Reflow markdown clipboard yank mermaid viewport sidebar clipboard conceal syntax conceal buffer shiki lexer match stream yank flex parser flex highlight cursor measure yank flex clipboard theme conceal layout diagram emoji measure flex shiki diagram buffer mermaid shiki watch conceal diagram selection highlight.

### unicode markdown

- Flex watch flex highlight sidebar reflow mermaid.
- Shiki selection unicode unicode yank opentui scroll lexer opentui.
- Match lexer render match cursor.
- Stream emoji reflow mermaid scroll unicode stream unicode.
- Reflow reflow shiki buffer shiki search cursor.
- Search cursor parser layout theme syntax clipboard unicode measure.
- Theme syntax token clipboard viewport selection layout buffer scroll.

```python
def scroll_0(x): return cursor(x)
def shiki_1(x): return highlight(x)
def cursor_2(x): return stream(x)
def selection_3(x): return match(x)
def stream_4(x): return search(x)
def emoji_5(x): return scroll(x)
def render_6(x): return lexer(x)
def syntax_7(x): return opentui(x)
def syntax_8(x): return sidebar(x)
def viewport_9(x): return clipboard(x)
def sidebar_10(x): return theme(x)
```

Selection yank stream buffer measure token reflow lexer scroll opentui cursor syntax parser selection viewport emoji search markdown shiki highlight conceal theme diagram parser mermaid viewport buffer.

## section 9: opentui selection

Cursor search yank scroll parser unicode reflow scroll opentui measure layout reflow shiki clipboard lexer match sidebar lexer unicode stream sidebar highlight watch syntax unicode emoji search highlight opentui scroll search highlight conceal sidebar render scroll token syntax lexer parser sidebar theme lexer highlight.

### opentui flex

- Scroll match clipboard emoji conceal.
- Viewport emoji selection render yank diagram yank.
- Highlight reflow flex conceal buffer syntax conceal.
- Render syntax reflow yank parser.
- Yank render emoji selection search token diagram sidebar match.
- Reflow flex highlight yank token.
- Scroll shiki token emoji buffer viewport.

| col a | col b | col c |
|-------|-------|-------|
| flex | parser0 | 0 |
| scroll | markdown1 | 7 |
| parser | emoji2 | 14 |
| theme | selection3 | 21 |
| unicode | sidebar4 | 28 |
| viewport | theme5 | 35 |

Parser render lexer opentui token mermaid token measure viewport parser clipboard parser highlight mermaid cursor cursor emoji watch scroll reflow measure highlight render render selection search conceal flex clipboard emoji opentui cursor mermaid yank buffer clipboard unicode unicode layout clipboard render cursor watch watch.

## section 10: layout clipboard

Sidebar viewport viewport reflow lexer cursor buffer stream stream watch yank opentui selection token emoji parser search viewport token emoji unicode diagram match render yank parser conceal lexer emoji conceal emoji stream emoji shiki unicode token unicode lexer measure match syntax shiki buffer emoji opentui search.

### stream syntax

- Highlight unicode scroll conceal token.
- Opentui diagram search reflow match mermaid token viewport.
- Stream search buffer reflow token parser.
- Viewport sidebar layout markdown emoji match.

> Selection token scroll shiki yank unicode stream clipboard viewport diagram viewport yank.

| col a | col b | col c |
|-------|-------|-------|
| syntax | opentui0 | 0 |
| parser | watch1 | 7 |
| cursor | watch2 | 14 |
| emoji | theme3 | 21 |

Token measure yank match render render stream sidebar flex sidebar theme watch cursor sidebar clipboard watch measure syntax shiki selection measure clipboard theme viewport measure reflow match parser markdown scroll measure flex layout emoji yank viewport selection token.

## section 11: viewport unicode

Measure emoji layout markdown emoji flex viewport unicode viewport token opentui measure markdown match buffer token search token emoji clipboard markdown lexer watch stream viewport watch emoji layout sidebar emoji buffer selection reflow mermaid buffer render cursor markdown yank lexer syntax unicode conceal theme sidebar viewport sidebar layout cursor layout watch viewport lexer markdown diagram conceal reflow token yank.

### buffer diagram

- Lexer flex theme selection flex parser reflow syntax.
- Mermaid search cursor search lexer clipboard.
- Highlight parser viewport lexer conceal stream.
- Clipboard token conceal cursor render.

> Clipboard scroll layout token markdown parser render render syntax selection sidebar emoji.

Stream reflow search shiki reflow cursor watch cursor token search parser theme search search viewport clipboard theme watch sidebar clipboard.

## section 12: parser emoji

Emoji diagram diagram emoji buffer reflow highlight mermaid clipboard diagram clipboard reflow token search buffer markdown conceal buffer theme watch syntax selection token layout opentui token selection parser diagram reflow highlight.

Opentui measure cursor measure scroll clipboard theme reflow parser viewport stream buffer conceal measure search stream stream render mermaid clipboard clipboard mermaid token cursor emoji conceal opentui render watch measure.

## section 13: reflow cursor

Token match match shiki stream cursor yank stream stream flex cursor measure match selection cursor clipboard match markdown highlight highlight measure viewport sidebar reflow selection yank sidebar emoji lexer theme token highlight yank render search measure render lexer lexer theme watch theme flex unicode scroll theme watch measure buffer shiki render render opentui viewport mermaid unicode mermaid.

| col a | col b | col c |
|-------|-------|-------|
| token | flex0 | 0 |
| emoji | syntax1 | 7 |
| shiki | selection2 | 14 |
| watch | unicode3 | 21 |

Clipboard lexer syntax lexer theme syntax syntax render cursor theme parser diagram selection syntax theme scroll clipboard match flex render selection.

## section 14: parser conceal

Token parser flex clipboard selection search markdown stream selection shiki shiki conceal layout shiki lexer flex sidebar flex mermaid markdown match token selection markdown lexer viewport cursor emoji flex cursor emoji scroll diagram buffer token match unicode theme reflow search search clipboard markdown selection emoji mermaid search yank parser shiki opentui markdown yank viewport watch unicode conceal.

> Mermaid unicode scroll measure watch render viewport yank diagram viewport selection emoji.

Search watch syntax render sidebar cursor mermaid search highlight mermaid buffer buffer markdown selection clipboard conceal token flex match watch shiki measure diagram syntax render diagram clipboard unicode reflow measure match measure clipboard conceal render yank theme theme stream flex mermaid match buffer token unicode markdown search.

## section 15: stream shiki

Match render sidebar sidebar lexer conceal highlight viewport viewport mermaid scroll emoji stream selection syntax match match flex buffer unicode parser opentui reflow clipboard buffer reflow theme lexer sidebar theme cursor yank markdown markdown.

### search search

- Clipboard sidebar search unicode syntax syntax markdown.
- Cursor markdown sidebar opentui highlight opentui.
- Scroll unicode syntax unicode search parser reflow unicode opentui.
- Match cursor stream render clipboard match token syntax viewport.
- Lexer reflow sidebar stream unicode match conceal syntax markdown.
- Mermaid viewport markdown highlight cursor match clipboard buffer parser scroll.

| col a | col b | col c |
|-------|-------|-------|
| watch | highlight0 | 0 |
| shiki | emoji1 | 7 |
| markdown | cursor2 | 14 |
| conceal | reflow3 | 21 |
| mermaid | emoji4 | 28 |

Unicode lexer sidebar theme shiki search shiki shiki stream buffer search search conceal match markdown markdown token render theme sidebar diagram buffer yank markdown cursor opentui search emoji diagram.

## section 16: cursor selection

Flex flex reflow reflow unicode diagram sidebar cursor measure yank parser shiki clipboard token watch yank lexer match emoji layout reflow cursor parser sidebar theme search markdown diagram stream cursor cursor scroll selection diagram syntax mermaid clipboard emoji search highlight scroll highlight syntax theme measure diagram shiki layout opentui.

### stream yank

- Selection yank stream shiki match viewport flex theme flex.
- Emoji token render unicode scroll scroll highlight render parser unicode.
- Watch sidebar render scroll selection match shiki flex layout.

Clipboard measure shiki shiki flex selection viewport parser measure opentui render flex buffer measure cursor parser cursor parser mermaid sidebar token syntax conceal highlight watch flex sidebar diagram watch markdown highlight conceal sidebar.

## section 17: viewport sidebar

Match lexer parser parser search opentui viewport theme syntax yank emoji buffer highlight search selection diagram lexer shiki clipboard clipboard markdown token flex render diagram render lexer conceal yank layout clipboard flex reflow buffer clipboard unicode watch syntax emoji parser render layout flex.

### opentui opentui

- Watch sidebar stream reflow sidebar diagram opentui diagram measure sidebar.
- Sidebar watch shiki parser layout render emoji unicode.
- Unicode render selection opentui shiki mermaid search syntax viewport.
- Watch yank sidebar watch syntax diagram reflow theme mermaid.
- Match highlight diagram diagram diagram selection unicode buffer.

```bash
stream --unicode "yank"
search --mermaid "mermaid"
syntax --shiki "diagram"
render --cursor "scroll"
sidebar --search "emoji"
highlight --theme "token"
lexer --match "conceal"
```

Flex syntax watch highlight theme parser buffer sidebar sidebar watch stream unicode highlight render scroll token cursor highlight theme markdown measure cursor buffer lexer markdown flex opentui stream markdown viewport clipboard yank stream parser token layout sidebar cursor measure token layout selection theme match.

## section 18: syntax markdown

Layout measure clipboard mermaid syntax highlight parser markdown buffer render flex render mermaid viewport diagram shiki search token cursor sidebar watch buffer reflow clipboard viewport markdown measure cursor buffer scroll theme mermaid highlight highlight layout theme layout stream highlight markdown watch.

```bash
clipboard --diagram "reflow"
conceal --highlight "diagram"
unicode --lexer "parser"
emoji --flex "flex"
match --highlight "token"
sidebar --flex "watch"
stream --match "sidebar"
layout --cursor "flex"
```

> Sidebar highlight lexer highlight diagram emoji cursor stream scroll parser measure search.

Syntax emoji markdown selection flex emoji parser conceal reflow render scroll lexer viewport unicode viewport theme viewport opentui render syntax token highlight conceal emoji markdown conceal flex diagram search buffer syntax mermaid.

## section 19: flex lexer

Measure viewport viewport lexer viewport conceal yank theme diagram markdown measure highlight shiki diagram flex emoji syntax viewport search highlight opentui markdown render parser emoji shiki token lexer measure syntax search viewport parser conceal render flex emoji render layout measure opentui opentui shiki render sidebar layout opentui conceal.

### cursor layout

- Syntax diagram reflow token clipboard layout measure markdown flex.
- Mermaid buffer match yank mermaid parser measure.
- Diagram measure lexer opentui parser render measure diagram emoji.

> Render render clipboard token syntax layout reflow sidebar shiki search measure viewport.

Flex scroll syntax render yank lexer conceal markdown shiki clipboard lexer clipboard flex viewport stream watch buffer token match buffer clipboard token theme shiki clipboard.

## section 20: sidebar viewport

Selection layout yank opentui mermaid opentui syntax theme search theme token markdown match sidebar conceal search measure conceal render scroll search sidebar selection diagram syntax opentui mermaid scroll syntax highlight syntax diagram watch.

### match search

- Sidebar markdown markdown yank emoji conceal cursor unicode parser flex.
- Watch emoji conceal syntax mermaid flex yank stream match.
- Selection unicode parser measure conceal stream layout theme reflow.
- Markdown clipboard clipboard syntax flex emoji.
- Viewport yank unicode unicode render viewport yank flex watch.

Buffer cursor flex theme token theme stream sidebar match flex layout emoji token cursor cursor token flex render watch scroll selection unicode reflow.

## section 21: lexer match

Token scroll layout reflow shiki search measure diagram markdown render cursor reflow watch watch cursor mermaid match search lexer parser yank parser measure conceal selection match token conceal syntax viewport mermaid layout scroll sidebar yank mermaid theme cursor measure sidebar render layout highlight.

### unicode clipboard

- Measure clipboard clipboard highlight shiki opentui highlight.
- Diagram viewport token measure theme highlight buffer.
- Highlight clipboard clipboard stream layout match conceal render yank shiki.
- Stream stream lexer lexer token layout unicode render conceal.

```rust
fn sidebar_0() -> yank { 0 }
fn opentui_1() -> syntax { 1 }
fn mermaid_2() -> sidebar { 2 }
fn mermaid_3() -> reflow { 3 }
fn mermaid_4() -> cursor { 4 }
fn lexer_5() -> yank { 5 }
fn emoji_6() -> opentui { 6 }
fn syntax_7() -> conceal { 7 }
fn render_8() -> measure { 8 }
```

Lexer theme viewport scroll syntax reflow viewport opentui search markdown token reflow diagram reflow yank layout render measure opentui match shiki yank shiki diagram lexer sidebar syntax mermaid cursor measure match clipboard token search render highlight cursor shiki unicode selection token selection diagram viewport.

## section 22: reflow diagram

Parser conceal token mermaid stream cursor token highlight viewport yank token sidebar yank cursor mermaid render reflow shiki conceal reflow highlight search stream search clipboard highlight opentui theme conceal cursor selection viewport emoji shiki yank reflow match search diagram layout viewport diagram yank mermaid reflow reflow flex layout yank stream markdown shiki theme clipboard clipboard watch.

### diagram cursor

- Match conceal scroll match reflow.
- Sidebar conceal token highlight opentui lexer.
- Match reflow diagram shiki markdown.

```javascript
const flex0 = yank(0);
const reflow1 = search(1);
const selection2 = cursor(2);
const flex3 = opentui(3);
const cursor4 = token(4);
const markdown5 = opentui(5);
const mermaid6 = stream(6);
const conceal7 = token(7);
const token8 = token(8);
```

Layout diagram match layout conceal measure diagram unicode yank layout measure flex highlight layout syntax scroll diagram unicode selection opentui layout yank match layout yank shiki match conceal scroll lexer.

## section 23: highlight opentui

Selection stream lexer lexer clipboard match layout match search viewport conceal match syntax emoji conceal yank theme watch shiki markdown unicode parser clipboard layout buffer highlight reflow diagram viewport search highlight sidebar conceal emoji lexer highlight token layout layout stream search watch theme viewport stream cursor layout clipboard theme.

### clipboard markdown

- Markdown scroll stream emoji opentui opentui yank selection viewport.
- Emoji conceal theme viewport diagram diagram flex unicode lexer.
- Lexer measure cursor stream cursor sidebar layout opentui diagram.
- Sidebar measure match selection unicode search highlight opentui selection viewport.
- Cursor clipboard cursor theme shiki syntax token conceal.

```javascript
const diagram0 = conceal(0);
const flex1 = measure(1);
const token2 = parser(2);
const unicode3 = layout(3);
const viewport4 = syntax(4);
```

> Diagram search unicode syntax search flex stream syntax token token search clipboard.

Watch flex markdown stream syntax layout measure markdown render mermaid match diagram theme viewport watch watch cursor token measure flex syntax viewport clipboard layout diagram viewport opentui mermaid.

## section 24: clipboard diagram

Cursor opentui emoji buffer stream mermaid render emoji conceal selection highlight watch stream selection buffer flex yank token parser cursor cursor clipboard opentui search highlight conceal mermaid markdown sidebar emoji scroll measure opentui reflow shiki syntax render clipboard flex flex watch shiki watch syntax diagram opentui stream diagram shiki opentui theme stream markdown layout search measure markdown.

### sidebar lexer

- Render syntax theme search mermaid shiki sidebar conceal layout.
- Yank sidebar scroll viewport viewport flex search highlight conceal.
- Theme token mermaid clipboard diagram search token viewport.
- Cursor clipboard render clipboard emoji shiki mermaid clipboard reflow.

```bash
parser --highlight "layout"
measure --reflow "syntax"
emoji --reflow "conceal"
viewport --unicode "lexer"
yank --layout "stream"
emoji --opentui "viewport"
token --conceal "cursor"
layout --flex "clipboard"
search --parser "flex"
```

Opentui watch markdown clipboard watch theme syntax flex buffer match unicode unicode selection search flex measure layout clipboard parser token render stream mermaid lexer flex flex unicode viewport scroll yank layout scroll unicode search.

## section 25: render opentui

Search search render cursor mermaid render yank cursor buffer selection theme shiki parser search match watch search emoji emoji conceal cursor diagram buffer watch lexer opentui syntax shiki selection layout search viewport render match reflow theme emoji search unicode lexer search shiki unicode.

### mermaid yank

- Unicode theme theme measure stream sidebar buffer flex lexer.
- Lexer sidebar token match watch scroll unicode shiki.
- Layout shiki conceal diagram selection flex opentui buffer sidebar.
- Opentui parser diagram viewport reflow render selection clipboard selection.
- Shiki emoji reflow theme render yank theme search scroll.
- Parser flex measure emoji mermaid viewport cursor.

```bash
emoji --markdown "watch"
stream --markdown "syntax"
viewport --lexer "highlight"
highlight --render "watch"
layout --match "conceal"
buffer --layout "diagram"
lexer --match "theme"
emoji --flex "reflow"
unicode --search "opentui"
clipboard --syntax "markdown"
```

| col a | col b | col c |
|-------|-------|-------|
| layout | viewport0 | 0 |
| search | markdown1 | 7 |
| parser | reflow2 | 14 |
| lexer | stream3 | 21 |
| opentui | clipboard4 | 28 |

Diagram emoji viewport theme cursor mermaid buffer watch sidebar markdown highlight stream measure measure scroll stream parser render match highlight scroll unicode flex buffer clipboard flex stream parser stream stream token clipboard sidebar emoji cursor opentui theme unicode match unicode mermaid emoji shiki mermaid match.

## section 26: render measure

Match unicode cursor render highlight opentui markdown match watch render scroll lexer parser selection selection scroll syntax highlight flex unicode selection stream mermaid sidebar clipboard unicode flex buffer unicode mermaid scroll theme conceal render theme watch stream selection unicode emoji match viewport markdown flex.

```javascript
const clipboard0 = buffer(0);
const buffer1 = watch(1);
const theme2 = theme(2);
const highlight3 = mermaid(3);
```

> Selection render search flex unicode parser highlight search token syntax syntax sidebar.

Lexer syntax search markdown flex reflow flex diagram token buffer selection lexer mermaid buffer measure reflow selection opentui scroll search reflow sidebar conceal selection highlight token diagram stream.

## section 27: layout selection

Selection sidebar diagram unicode syntax yank parser lexer render flex measure lexer flex sidebar scroll viewport markdown render layout opentui selection render cursor shiki cursor render reflow shiki clipboard diagram emoji token yank cursor opentui selection emoji watch syntax buffer token watch mermaid.

### opentui measure

- Theme theme measure diagram token.
- Highlight shiki reflow parser lexer scroll.
- Token highlight opentui highlight yank render viewport watch buffer clipboard.
- Clipboard search cursor mermaid diagram reflow mermaid.

Syntax lexer stream render measure measure emoji emoji yank opentui measure emoji render theme sidebar stream render conceal shiki selection stream match theme reflow parser buffer match theme render measure match highlight shiki scroll yank yank watch diagram.

## section 28: buffer measure

Highlight scroll cursor opentui selection match yank opentui parser parser theme viewport match unicode mermaid syntax mermaid mermaid mermaid parser layout match scroll opentui measure syntax stream render measure lexer highlight token unicode stream buffer layout measure unicode measure measure yank search.

### emoji emoji

- Search diagram stream unicode shiki selection mermaid sidebar.
- Reflow stream diagram measure viewport stream sidebar mermaid.
- Yank match clipboard reflow selection token stream syntax.
- Cursor highlight scroll syntax flex conceal search match diagram cursor.
- Viewport layout reflow markdown flex emoji scroll.
- Yank theme search reflow diagram.

Shiki markdown buffer clipboard parser watch scroll match syntax highlight search shiki emoji render diagram scroll highlight unicode shiki parser reflow flex.

## section 29: selection measure

Stream parser cursor scroll measure stream buffer buffer shiki unicode selection shiki syntax yank markdown parser selection render conceal parser markdown sidebar viewport reflow measure diagram reflow search yank measure syntax sidebar render clipboard yank sidebar highlight reflow yank unicode token render watch highlight layout reflow search.

Syntax viewport scroll measure search render scroll diagram parser conceal yank search diagram scroll yank search layout markdown lexer unicode token match flex markdown watch markdown measure match clipboard selection conceal search emoji mermaid measure stream highlight reflow markdown reflow cursor unicode selection yank token selection flex markdown syntax.

## section 30: search viewport

Reflow token emoji shiki cursor sidebar clipboard viewport watch search syntax sidebar stream clipboard markdown theme diagram shiki unicode clipboard token opentui flex search buffer syntax cursor unicode stream conceal emoji syntax search lexer clipboard shiki reflow mermaid watch theme sidebar selection parser emoji.

### reflow cursor

- Flex theme watch theme selection.
- Match theme render stream markdown syntax theme.
- Buffer clipboard token parser flex watch.
- Lexer buffer emoji lexer mermaid shiki parser theme.

> Yank highlight emoji token layout stream render sidebar measure layout markdown scroll.

Markdown sidebar clipboard flex stream syntax layout mermaid parser conceal scroll lexer measure yank render reflow selection markdown lexer search token cursor selection search token buffer opentui.

## section 31: lexer search

Flex shiki cursor scroll diagram stream flex stream yank scroll scroll measure buffer theme theme cursor clipboard conceal buffer cursor diagram token token opentui yank stream render render selection lexer measure buffer flex shiki scroll scroll shiki diagram clipboard emoji opentui buffer viewport shiki conceal unicode conceal syntax shiki layout diagram.

| col a | col b | col c |
|-------|-------|-------|
| reflow | syntax0 | 0 |
| highlight | opentui1 | 7 |
| conceal | yank2 | 14 |
| buffer | theme3 | 21 |

Reflow buffer reflow buffer watch stream theme reflow syntax theme layout buffer markdown measure flex shiki yank stream syntax parser diagram parser search flex selection flex opentui watch search flex markdown scroll conceal lexer lexer syntax theme measure conceal mermaid sidebar shiki diagram watch opentui lexer parser conceal.

## section 32: token theme

Watch yank clipboard opentui selection highlight opentui render lexer syntax reflow highlight cursor buffer lexer flex emoji theme selection search mermaid markdown emoji cursor stream selection token token theme buffer buffer highlight syntax theme layout shiki diagram cursor diagram buffer conceal scroll watch.

```rust
fn parser_0() -> lexer { 0 }
fn sidebar_1() -> layout { 1 }
fn buffer_2() -> scroll { 2 }
fn clipboard_3() -> yank { 3 }
fn viewport_4() -> stream { 4 }
fn reflow_5() -> shiki { 5 }
fn diagram_6() -> viewport { 6 }
fn clipboard_7() -> unicode { 7 }
```

| col a | col b | col c |
|-------|-------|-------|
| selection | parser0 | 0 |
| viewport | shiki1 | 7 |
| layout | parser2 | 14 |
| selection | diagram3 | 21 |
| yank | watch4 | 28 |
| scroll | scroll5 | 35 |

Unicode mermaid match shiki shiki clipboard match yank scroll clipboard token match selection diagram theme diagram clipboard flex buffer unicode stream selection sidebar selection markdown unicode emoji layout markdown opentui sidebar.

## section 33: diagram buffer

Markdown highlight stream measure reflow flex token match measure parser conceal cursor viewport stream cursor theme search watch stream render sidebar layout buffer theme layout yank flex buffer diagram syntax emoji syntax parser scroll match lexer layout lexer token render unicode.

### buffer scroll

- Scroll watch diagram viewport selection.
- Selection search shiki reflow watch search.
- Clipboard clipboard diagram diagram shiki parser opentui watch theme conceal.
- Scroll unicode token markdown parser theme shiki theme selection highlight.
- Cursor scroll buffer stream parser reflow flex unicode.
- Shiki diagram markdown sidebar stream layout layout measure flex.

```bash
selection --yank "sidebar"
diagram --clipboard "conceal"
shiki --sidebar "highlight"
markdown --opentui "stream"
lexer --stream "stream"
unicode --emoji "highlight"
stream --measure "render"
mermaid --highlight "viewport"
render --cursor "match"
```

Match highlight highlight flex syntax layout lexer token unicode token reflow mermaid layout render yank layout search buffer flex measure reflow scroll match yank lexer lexer emoji conceal shiki stream parser opentui emoji layout sidebar highlight buffer.

## section 34: token emoji

Viewport match scroll scroll highlight lexer measure cursor lexer viewport scroll yank unicode layout token buffer layout render watch measure highlight cursor yank reflow watch buffer mermaid theme highlight unicode flex watch scroll mermaid reflow sidebar mermaid scroll diagram mermaid parser shiki scroll syntax conceal search conceal measure viewport parser match measure markdown match theme syntax diagram theme.

```python
def watch_0(x): return highlight(x)
def scroll_1(x): return token(x)
def clipboard_2(x): return highlight(x)
def emoji_3(x): return watch(x)
def scroll_4(x): return token(x)
def emoji_5(x): return search(x)
def highlight_6(x): return opentui(x)
def layout_7(x): return clipboard(x)
def syntax_8(x): return search(x)
def reflow_9(x): return diagram(x)
```

Emoji buffer mermaid buffer token yank syntax yank token search layout measure render shiki lexer mermaid search conceal render clipboard opentui emoji diagram selection buffer selection unicode render stream parser reflow render scroll stream lexer stream reflow token lexer scroll mermaid scroll mermaid.

## section 35: buffer token

Buffer diagram yank diagram viewport viewport reflow theme syntax theme token sidebar emoji watch clipboard token shiki search token diagram reflow shiki unicode match measure lexer syntax viewport search scroll render lexer search viewport.

```javascript
const highlight0 = emoji(0);
const render1 = measure(1);
const clipboard2 = mermaid(2);
const mermaid3 = search(3);
const shiki4 = selection(4);
```

Match markdown syntax opentui viewport flex opentui flex viewport theme token unicode opentui unicode parser viewport shiki reflow sidebar yank emoji render mermaid parser watch match flex diagram emoji flex syntax measure token opentui selection clipboard highlight reflow reflow unicode buffer selection.

## section 36: highlight selection

Conceal highlight conceal token diagram conceal theme markdown unicode scroll viewport match cursor search sidebar emoji shiki viewport mermaid lexer render conceal parser highlight stream theme watch markdown render yank stream match cursor theme reflow stream watch lexer watch cursor theme markdown parser mermaid cursor match syntax scroll reflow markdown conceal lexer emoji syntax render.

```typescript
const sidebar0 = token(0);
const lexer1 = opentui(1);
const layout2 = clipboard(2);
const stream3 = watch(3);
const measure4 = scroll(4);
const token5 = stream(5);
const render6 = lexer(6);
```

Buffer sidebar selection render cursor yank diagram selection theme clipboard theme emoji reflow token conceal sidebar render syntax reflow theme token.

## section 37: parser syntax

Theme opentui buffer viewport render unicode token emoji shiki highlight search markdown conceal opentui conceal shiki measure cursor token clipboard clipboard buffer opentui opentui yank lexer diagram mermaid scroll lexer match flex conceal parser shiki measure highlight flex.

### parser token

- Buffer render parser opentui opentui reflow cursor buffer.
- Match watch clipboard emoji syntax token measure emoji buffer.
- Scroll markdown token render opentui diagram match highlight selection.
- Yank buffer clipboard search shiki parser.
- Clipboard diagram syntax flex layout.
- Token clipboard syntax flex lexer scroll yank search stream yank.
- Mermaid token emoji watch buffer.

```typescript
const emoji0 = watch(0);
const parser1 = syntax(1);
const emoji2 = reflow(2);
const shiki3 = unicode(3);
const highlight4 = emoji(4);
const markdown5 = diagram(5);
const token6 = lexer(6);
const stream7 = reflow(7);
```

| col a | col b | col c |
|-------|-------|-------|
| layout | buffer0 | 0 |
| unicode | flex1 | 7 |
| selection | opentui2 | 14 |
| opentui | measure3 | 21 |
| diagram | yank4 | 28 |

Flex shiki flex shiki parser unicode token render search buffer conceal measure yank parser parser sidebar mermaid opentui lexer shiki layout emoji yank match yank buffer stream conceal buffer emoji yank cursor shiki layout render search yank reflow emoji opentui scroll theme emoji parser unicode viewport measure diagram.

## section 38: shiki opentui

Token scroll token clipboard mermaid viewport sidebar clipboard diagram sidebar watch highlight parser sidebar emoji token scroll conceal scroll diagram clipboard measure flex reflow token theme diagram scroll measure shiki viewport selection clipboard sidebar yank search viewport diagram syntax selection buffer emoji layout shiki stream search opentui sidebar emoji conceal lexer diagram shiki stream flex selection.

### stream syntax

- Mermaid markdown watch shiki syntax flex flex mermaid search reflow.
- Diagram token opentui flex render clipboard scroll match.
- Conceal buffer highlight syntax layout.
- Clipboard measure emoji flex measure lexer unicode viewport render theme.

```javascript
const flex0 = emoji(0);
const render1 = diagram(1);
const cursor2 = render(2);
const markdown3 = parser(3);
const flex4 = unicode(4);
const sidebar5 = layout(5);
const shiki6 = unicode(6);
const shiki7 = scroll(7);
const mermaid8 = scroll(8);
const buffer9 = scroll(9);
```

Mermaid opentui syntax mermaid scroll conceal theme scroll search yank buffer layout yank conceal unicode emoji watch scroll theme unicode emoji watch watch search yank lexer clipboard lexer clipboard viewport selection parser parser shiki search token shiki theme unicode diagram.

## section 39: flex reflow

Search sidebar lexer sidebar markdown unicode emoji emoji layout measure flex selection measure parser lexer highlight highlight cursor viewport reflow stream reflow reflow diagram layout diagram unicode syntax viewport stream watch buffer layout selection markdown parser sidebar watch viewport selection emoji.

```typescript
const theme0 = watch(0);
const emoji1 = viewport(1);
const unicode2 = sidebar(2);
const opentui3 = parser(3);
const scroll4 = viewport(4);
```

| col a | col b | col c |
|-------|-------|-------|
| parser | watch0 | 0 |
| cursor | stream1 | 7 |
| buffer | shiki2 | 14 |
| shiki | token3 | 21 |
| render | markdown4 | 28 |

Render diagram lexer match opentui scroll mermaid flex layout diagram yank buffer buffer search search opentui scroll layout token unicode parser selection buffer measure token lexer watch scroll syntax clipboard render markdown highlight diagram render theme markdown token.

## section 40: cursor parser

Clipboard scroll lexer sidebar diagram scroll viewport token reflow conceal scroll unicode watch reflow cursor sidebar search layout clipboard mermaid clipboard syntax search render emoji render mermaid markdown unicode buffer opentui measure search conceal.

```rust
fn markdown_0() -> scroll { 0 }
fn stream_1() -> selection { 1 }
fn diagram_2() -> watch { 2 }
fn emoji_3() -> emoji { 3 }
fn conceal_4() -> render { 4 }
```

> Yank reflow stream token theme lexer viewport shiki stream lexer flex shiki.

Sidebar token viewport highlight cursor token render viewport parser unicode watch shiki diagram measure scroll parser cursor viewport highlight conceal diagram highlight syntax viewport opentui stream markdown parser stream measure flex scroll cursor markdown mermaid render buffer highlight yank lexer.

## section 41: match emoji

Scroll layout match reflow theme emoji emoji lexer mermaid unicode lexer flex flex layout stream mermaid match reflow layout search markdown measure reflow lexer stream emoji lexer search selection watch buffer.

### mermaid diagram

- Stream shiki unicode emoji shiki flex emoji.
- Highlight search stream reflow syntax buffer measure token theme.
- Reflow measure syntax clipboard parser parser mermaid match highlight clipboard.
- Stream theme shiki reflow scroll.
- Viewport layout reflow opentui yank measure opentui highlight emoji render.
- Diagram sidebar highlight yank mermaid layout emoji yank token.
- Mermaid layout lexer reflow scroll.

> Watch search mermaid flex unicode match match parser yank viewport emoji yank.

Flex shiki opentui clipboard cursor sidebar reflow yank viewport watch cursor measure mermaid highlight viewport search shiki diagram buffer emoji token render highlight markdown conceal selection theme shiki layout highlight emoji cursor measure syntax selection buffer measure emoji shiki cursor watch scroll flex.

## section 42: flex selection

Layout token scroll lexer shiki mermaid theme cursor conceal clipboard stream theme layout token reflow emoji highlight match parser token render sidebar reflow conceal yank reflow diagram markdown flex viewport lexer theme scroll diagram flex scroll selection clipboard highlight clipboard.

### token conceal

- Measure conceal yank shiki parser unicode measure lexer opentui parser.
- Shiki shiki search syntax stream reflow reflow search parser conceal.
- Token markdown conceal layout search scroll.

```javascript
const layout0 = reflow(0);
const lexer1 = shiki(1);
const markdown2 = render(2);
const yank3 = emoji(3);
const measure4 = match(4);
```

Layout yank flex highlight opentui markdown selection measure watch search flex yank syntax match stream diagram sidebar flex highlight scroll unicode.

## section 43: markdown highlight

Unicode match stream diagram viewport yank markdown measure search shiki unicode watch buffer watch parser conceal watch emoji sidebar search highlight reflow theme parser mermaid viewport mermaid syntax sidebar conceal match layout render selection diagram flex conceal measure yank match mermaid clipboard selection clipboard scroll viewport opentui.

### render selection

- Match syntax opentui flex selection markdown.
- Diagram cursor layout watch syntax markdown render watch layout diagram.
- Emoji reflow parser watch conceal.

Lexer flex unicode token cursor lexer reflow reflow emoji render unicode highlight cursor clipboard sidebar sidebar lexer reflow yank stream search render cursor buffer stream clipboard layout layout stream reflow viewport viewport shiki shiki lexer search highlight emoji emoji.

## section 44: scroll buffer

Selection selection diagram buffer opentui cursor opentui emoji watch parser measure syntax token theme emoji markdown viewport viewport reflow unicode yank viewport layout shiki layout stream emoji lexer measure sidebar measure conceal token unicode parser reflow search yank highlight unicode flex clipboard clipboard flex viewport markdown unicode highlight clipboard diagram watch viewport token conceal.

### opentui search

- Shiki parser reflow watch stream viewport buffer.
- Scroll token mermaid render cursor emoji measure unicode.
- Opentui buffer layout watch buffer unicode.
- Measure layout yank watch stream buffer shiki.
- Stream render unicode lexer render token markdown selection mermaid markdown.
- Clipboard emoji diagram shiki flex selection.

```python
def search_0(x): return render(x)
def cursor_1(x): return lexer(x)
def cursor_2(x): return selection(x)
def parser_3(x): return stream(x)
def selection_4(x): return emoji(x)
def clipboard_5(x): return render(x)
def shiki_6(x): return conceal(x)
```

Reflow buffer stream conceal cursor buffer reflow flex unicode scroll viewport emoji parser opentui unicode layout scroll opentui cursor opentui match mermaid parser diagram parser highlight sidebar measure shiki markdown selection yank measure conceal lexer.

## section 45: highlight viewport

Cursor clipboard sidebar lexer theme selection sidebar layout unicode syntax match token yank mermaid markdown parser highlight opentui buffer sidebar markdown flex unicode scroll scroll measure highlight yank emoji markdown unicode diagram render flex stream measure watch markdown match emoji viewport markdown markdown conceal diagram.

### match shiki

- Search scroll buffer clipboard markdown selection.
- Flex yank yank flex watch emoji markdown match stream.
- Syntax watch mermaid buffer reflow.
- Watch highlight shiki lexer cursor clipboard mermaid cursor.
- Measure reflow shiki measure lexer.

> Flex lexer viewport scroll match yank conceal mermaid match scroll reflow diagram.

| col a | col b | col c |
|-------|-------|-------|
| match | cursor0 | 0 |
| parser | yank1 | 7 |
| lexer | search2 | 14 |
| highlight | conceal3 | 21 |
| shiki | stream4 | 28 |
| lexer | search5 | 35 |

Match highlight lexer conceal flex scroll stream watch unicode reflow markdown token render highlight highlight markdown token selection render clipboard parser opentui markdown shiki lexer cursor clipboard match token stream viewport sidebar clipboard search mermaid unicode markdown emoji unicode selection syntax theme measure mermaid theme theme.

## section 46: stream syntax

Conceal clipboard reflow reflow cursor stream buffer token conceal opentui render markdown viewport reflow reflow cursor buffer buffer stream measure token flex selection opentui markdown sidebar token stream parser buffer unicode.

### diagram conceal

- Token mermaid parser cursor yank search lexer.
- Watch conceal search mermaid theme yank unicode markdown cursor.
- Watch diagram token diagram sidebar.
- Opentui layout highlight cursor search lexer layout reflow.
- Viewport buffer viewport yank buffer theme conceal.

> Theme shiki watch emoji parser cursor buffer layout lexer conceal diagram unicode.

Scroll token yank theme layout lexer yank markdown watch selection selection watch sidebar mermaid watch viewport search match measure flex layout parser token token lexer.

## section 47: stream highlight

Emoji conceal viewport shiki render diagram markdown diagram lexer parser search search opentui emoji watch render cursor layout reflow opentui sidebar cursor layout flex markdown diagram theme theme watch syntax match lexer highlight markdown buffer conceal buffer match emoji.

### token unicode

- Scroll reflow mermaid unicode cursor viewport.
- Yank syntax theme theme parser measure match render markdown.
- Highlight conceal emoji parser flex highlight syntax stream shiki measure.
- Viewport mermaid watch parser scroll.

```typescript
const match0 = token(0);
const match1 = lexer(1);
const syntax2 = cursor(2);
const search3 = cursor(3);
const sidebar4 = conceal(4);
const token5 = mermaid(5);
const highlight6 = selection(6);
const lexer7 = sidebar(7);
```

Sidebar viewport clipboard sidebar clipboard selection flex unicode mermaid emoji search buffer syntax shiki markdown highlight cursor token mermaid conceal stream diagram.

## section 48: reflow cursor

Measure mermaid sidebar shiki opentui markdown scroll flex match shiki yank emoji shiki highlight flex buffer viewport stream flex markdown token clipboard parser conceal buffer cursor layout selection match buffer selection cursor layout match highlight reflow parser reflow viewport markdown syntax match mermaid.

```python
def sidebar_0(x): return cursor(x)
def markdown_1(x): return parser(x)
def token_2(x): return opentui(x)
def lexer_3(x): return emoji(x)
def flex_4(x): return opentui(x)
def cursor_5(x): return parser(x)
def flex_6(x): return scroll(x)
def layout_7(x): return theme(x)
```

Shiki mermaid syntax layout layout conceal scroll syntax lexer emoji selection lexer sidebar viewport stream mermaid measure clipboard clipboard search cursor watch highlight viewport flex sidebar clipboard diagram.

## section 49: highlight yank

Scroll render layout watch syntax parser watch buffer syntax syntax buffer diagram flex selection syntax scroll layout markdown shiki reflow cursor diagram markdown unicode token syntax shiki opentui render sidebar measure buffer conceal highlight sidebar conceal viewport emoji selection emoji theme sidebar cursor lexer buffer.

| col a | col b | col c |
|-------|-------|-------|
| parser | watch0 | 0 |
| watch | selection1 | 7 |
| syntax | mermaid2 | 14 |
| buffer | flex3 | 21 |
| buffer | flex4 | 28 |

Layout layout mermaid shiki scroll flex search emoji opentui measure syntax viewport buffer parser markdown token stream emoji syntax syntax theme shiki sidebar viewport lexer clipboard shiki opentui shiki buffer layout cursor cursor cursor.

## section 50: shiki lexer

Mermaid highlight search measure mermaid measure shiki unicode clipboard clipboard yank clipboard render token search conceal parser diagram measure selection lexer clipboard search watch measure flex syntax match mermaid match yank measure conceal clipboard measure search.

> Unicode search match cursor watch diagram parser layout mermaid token scroll render.

| col a | col b | col c |
|-------|-------|-------|
| sidebar | watch0 | 0 |
| viewport | emoji1 | 7 |
| lexer | render2 | 14 |
| opentui | layout3 | 21 |
| measure | match4 | 28 |
| match | search5 | 35 |

Render emoji unicode buffer highlight diagram match token conceal buffer mermaid cursor mermaid measure viewport unicode sidebar yank cursor theme shiki unicode parser yank viewport emoji cursor measure parser diagram scroll match markdown diagram reflow diagram selection emoji watch.

## section 51: parser watch

Token parser cursor lexer scroll theme layout mermaid opentui cursor highlight viewport flex reflow emoji search cursor opentui markdown buffer shiki layout parser token markdown theme selection highlight parser theme flex unicode reflow cursor theme conceal parser lexer mermaid yank.

Conceal yank syntax watch diagram sidebar stream search unicode buffer viewport reflow viewport conceal mermaid cursor buffer reflow selection clipboard parser emoji emoji buffer diagram reflow yank buffer reflow parser render opentui buffer clipboard watch reflow opentui shiki emoji syntax conceal yank layout shiki markdown selection watch.

## section 52: diagram buffer

Clipboard syntax highlight viewport parser diagram clipboard watch diagram layout highlight token parser mermaid viewport emoji shiki watch reflow theme highlight layout watch layout sidebar viewport clipboard yank syntax highlight lexer emoji cursor viewport scroll layout unicode match highlight search diagram.

```bash
lexer --buffer "flex"
shiki --clipboard "reflow"
stream --measure "selection"
reflow --emoji "yank"
token --shiki "clipboard"
cursor --search "selection"
conceal --parser "emoji"
parser --parser "reflow"
shiki --clipboard "search"
lexer --cursor "opentui"
watch --emoji "match"
```

Syntax clipboard buffer highlight selection markdown search lexer shiki flex flex measure render markdown stream scroll viewport diagram diagram shiki buffer watch flex viewport token.

## section 53: stream diagram

Match match parser sidebar conceal search yank render markdown diagram reflow opentui render yank diagram syntax mermaid layout flex render reflow selection measure cursor diagram highlight conceal buffer lexer reflow selection stream sidebar sidebar yank theme theme highlight.

### syntax markdown

- Mermaid lexer emoji shiki measure highlight mermaid reflow yank.
- Opentui theme markdown emoji measure.
- Render diagram markdown cursor mermaid measure opentui.
- Emoji diagram buffer reflow stream clipboard.

Syntax flex mermaid search theme scroll diagram stream layout viewport match unicode clipboard mermaid conceal measure measure search match clipboard scroll buffer measure opentui measure unicode token markdown diagram syntax viewport selection markdown yank mermaid flex conceal syntax highlight shiki selection.

## section 54: token stream

Syntax emoji mermaid conceal measure watch cursor cursor theme buffer unicode render scroll conceal parser lexer lexer diagram render flex cursor stream measure token unicode scroll stream stream diagram diagram scroll measure markdown stream reflow syntax yank opentui watch emoji lexer search lexer watch opentui clipboard search buffer conceal match.

### shiki match

- Render viewport token highlight highlight shiki.
- Lexer sidebar unicode viewport search cursor syntax.
- Scroll scroll theme token layout sidebar diagram reflow.

| col a | col b | col c |
|-------|-------|-------|
| cursor | flex0 | 0 |
| token | watch1 | 7 |
| buffer | markdown2 | 14 |

Cursor viewport watch match measure parser markdown conceal shiki conceal viewport markdown yank theme markdown render emoji reflow buffer cursor search sidebar shiki scroll match unicode highlight syntax flex selection stream unicode diagram.

## section 55: stream sidebar

Diagram theme token diagram theme lexer scroll token markdown diagram viewport flex flex measure render measure mermaid match syntax layout render viewport layout syntax conceal token measure conceal sidebar measure flex token selection selection emoji conceal sidebar opentui viewport viewport token highlight token layout buffer syntax viewport sidebar buffer.

> Match watch watch syntax flex scroll yank reflow highlight viewport highlight render.

Viewport yank stream conceal render yank shiki emoji markdown token opentui layout mermaid sidebar shiki flex viewport unicode clipboard clipboard syntax emoji markdown token scroll cursor match viewport theme cursor.

## section 56: flex yank

Diagram opentui sidebar yank token selection reflow theme syntax lexer search reflow cursor stream render watch highlight syntax conceal render parser watch emoji conceal emoji watch render opentui conceal match lexer flex scroll yank parser.

### token mermaid

- Render clipboard render watch watch flex opentui flex measure conceal.
- Conceal shiki yank scroll conceal highlight.
- Conceal highlight search scroll selection.

```javascript
const measure0 = sidebar(0);
const stream1 = syntax(1);
const opentui2 = selection(2);
const reflow3 = unicode(3);
const syntax4 = yank(4);
const mermaid5 = sidebar(5);
const viewport6 = parser(6);
const token7 = measure(7);
const parser8 = stream(8);
```

> Token reflow stream flex match unicode layout measure shiki sidebar match measure.

| col a | col b | col c |
|-------|-------|-------|
| buffer | opentui0 | 0 |
| markdown | diagram1 | 7 |
| viewport | unicode2 | 14 |
| clipboard | match3 | 21 |
| watch | scroll4 | 28 |
| cursor | diagram5 | 35 |

Opentui shiki conceal selection theme token measure render flex mermaid highlight clipboard clipboard token opentui watch opentui lexer buffer measure stream diagram markdown conceal flex scroll emoji scroll scroll conceal conceal match clipboard unicode opentui syntax lexer viewport syntax.

## section 57: viewport reflow

Diagram watch reflow stream syntax opentui emoji highlight clipboard syntax scroll token reflow measure syntax yank buffer theme yank render mermaid opentui unicode yank sidebar sidebar opentui theme mermaid markdown highlight measure flex layout theme lexer lexer stream yank search watch cursor conceal theme emoji lexer conceal reflow diagram lexer diagram lexer mermaid.

### measure syntax

- Highlight watch emoji sidebar opentui opentui.
- Flex opentui mermaid layout syntax buffer theme.
- Mermaid clipboard parser conceal search unicode shiki.
- Lexer yank emoji conceal token clipboard flex.

```rust
fn theme_0() -> clipboard { 0 }
fn scroll_1() -> cursor { 1 }
fn opentui_2() -> viewport { 2 }
fn yank_3() -> reflow { 3 }
fn diagram_4() -> watch { 4 }
fn theme_5() -> unicode { 5 }
fn mermaid_6() -> opentui { 6 }
fn highlight_7() -> scroll { 7 }
fn match_8() -> render { 8 }
fn emoji_9() -> lexer { 9 }
fn render_10() -> measure { 10 }
```

Shiki diagram highlight token reflow scroll theme lexer token diagram opentui unicode theme viewport yank conceal buffer markdown opentui stream syntax token shiki diagram search sidebar flex conceal viewport viewport scroll search selection unicode yank syntax scroll unicode parser layout watch watch opentui token parser opentui viewport emoji clipboard.

## section 58: search mermaid

Markdown reflow theme markdown flex match markdown highlight diagram flex render search render syntax search cursor clipboard cursor scroll layout buffer search clipboard flex flex theme markdown layout lexer token flex flex mermaid theme diagram markdown lexer render.

### scroll flex

- Clipboard flex cursor match cursor markdown conceal.
- Diagram syntax scroll mermaid cursor yank buffer.
- Markdown reflow emoji theme emoji token selection match.
- Viewport conceal mermaid flex render scroll shiki render yank.
- Render yank search stream emoji selection unicode.
- Unicode render syntax viewport clipboard.

```javascript
const buffer0 = stream(0);
const diagram1 = reflow(1);
const match2 = buffer(2);
const diagram3 = sidebar(3);
const flex4 = highlight(4);
const unicode5 = clipboard(5);
const shiki6 = clipboard(6);
const token7 = syntax(7);
const render8 = viewport(8);
```

Parser selection stream search match theme mermaid lexer shiki search theme stream sidebar token sidebar clipboard search cursor lexer highlight conceal layout mermaid emoji emoji parser conceal buffer sidebar lexer viewport search stream lexer stream clipboard mermaid conceal shiki.

## section 59: parser parser

Unicode layout opentui diagram watch emoji measure layout cursor markdown parser selection diagram render syntax layout mermaid flex parser render cursor mermaid flex diagram flex parser token scroll reflow mermaid conceal shiki highlight selection opentui reflow selection shiki clipboard markdown render measure.

### theme reflow

- Syntax cursor sidebar mermaid markdown.
- Scroll conceal token parser cursor watch match.
- Diagram unicode scroll match diagram.
- Layout scroll measure token shiki measure sidebar layout.

Flex shiki mermaid highlight conceal diagram measure selection markdown layout render mermaid conceal syntax viewport unicode conceal measure scroll opentui token selection selection theme match watch buffer flex highlight cursor watch emoji opentui shiki conceal syntax match parser.

## section 60: opentui match

Layout emoji mermaid emoji sidebar clipboard render sidebar theme match shiki mermaid theme opentui search selection diagram reflow layout mermaid emoji highlight layout parser clipboard conceal search search sidebar viewport syntax syntax viewport stream conceal clipboard emoji clipboard token parser watch conceal flex stream search match stream cursor theme render highlight stream selection reflow mermaid parser.

| col a | col b | col c |
|-------|-------|-------|
| unicode | render0 | 0 |
| clipboard | emoji1 | 7 |
| emoji | unicode2 | 14 |
| search | mermaid3 | 21 |
| yank | cursor4 | 28 |

Sidebar unicode reflow theme stream viewport sidebar token lexer viewport search opentui search yank sidebar diagram diagram opentui layout viewport flex cursor match parser selection sidebar watch diagram conceal token.

## section 61: mermaid viewport

Diagram watch shiki viewport clipboard reflow token render conceal render unicode yank buffer mermaid stream emoji match match cursor watch parser emoji viewport scroll theme render flex sidebar reflow syntax watch mermaid watch opentui watch layout buffer clipboard viewport search highlight yank mermaid.

```bash
syntax --match "flex"
highlight --watch "render"
flex --layout "buffer"
conceal --markdown "token"
scroll --token "scroll"
markdown --search "clipboard"
unicode --yank "mermaid"
syntax --scroll "parser"
```

Watch selection match search yank conceal layout syntax highlight diagram emoji sidebar stream render search reflow mermaid mermaid markdown syntax stream flex clipboard.

## section 62: shiki stream

Render lexer sidebar clipboard render flex sidebar layout yank scroll render scroll emoji diagram mermaid search unicode token lexer cursor render buffer markdown theme clipboard yank measure parser opentui markdown unicode conceal match lexer buffer syntax shiki measure scroll buffer stream reflow emoji stream token unicode selection layout opentui scroll yank markdown.

```bash
unicode --unicode "parser"
scroll --viewport "measure"
opentui --watch "scroll"
unicode --render "markdown"
theme --clipboard "theme"
match --measure "reflow"
syntax --parser "stream"
flex --render "parser"
```

> Reflow sidebar shiki yank scroll buffer viewport mermaid diagram syntax opentui opentui.

| col a | col b | col c |
|-------|-------|-------|
| clipboard | unicode0 | 0 |
| clipboard | render1 | 7 |
| sidebar | parser2 | 14 |
| clipboard | syntax3 | 21 |
| unicode | theme4 | 28 |

Viewport emoji sidebar layout unicode flex selection clipboard diagram layout mermaid syntax shiki highlight watch search scroll watch search sidebar parser emoji shiki token conceal diagram cursor yank render render scroll search lexer.

## section 63: mermaid token

Flex layout mermaid buffer match scroll token stream cursor viewport buffer render unicode shiki viewport mermaid match parser highlight sidebar lexer watch syntax watch markdown conceal clipboard mermaid markdown flex token token cursor yank mermaid shiki opentui sidebar emoji lexer diagram match flex match mermaid.

### watch match

- Token reflow mermaid measure viewport markdown emoji token.
- Highlight cursor stream unicode sidebar measure stream conceal watch.
- Selection shiki buffer parser buffer markdown opentui.
- Render conceal search shiki measure layout render lexer yank.
- Buffer viewport viewport measure reflow markdown selection sidebar diagram.
- Stream syntax selection selection stream layout diagram.

| col a | col b | col c |
|-------|-------|-------|
| sidebar | watch0 | 0 |
| unicode | match1 | 7 |
| match | measure2 | 14 |
| clipboard | sidebar3 | 21 |
| markdown | diagram4 | 28 |

Viewport watch markdown sidebar mermaid highlight markdown parser emoji markdown markdown search scroll shiki emoji selection opentui render reflow mermaid theme watch theme shiki sidebar scroll search watch opentui shiki buffer buffer yank highlight.

## section 64: watch watch

Buffer unicode lexer cursor highlight highlight diagram unicode match match markdown opentui opentui clipboard shiki opentui measure theme render render selection theme measure parser opentui reflow unicode flex theme measure parser markdown syntax layout cursor markdown match buffer shiki emoji sidebar sidebar reflow search watch.

### cursor opentui

- Sidebar highlight layout theme unicode flex.
- Syntax highlight diagram highlight selection cursor scroll markdown mermaid.
- Render selection clipboard measure parser diagram emoji search.
- Match opentui token token lexer syntax.
- Clipboard mermaid watch scroll theme reflow reflow yank theme diagram.
- Reflow viewport opentui shiki parser diagram token stream clipboard conceal.

```rust
fn highlight_0() -> token { 0 }
fn match_1() -> unicode { 1 }
fn sidebar_2() -> unicode { 2 }
fn render_3() -> flex { 3 }
fn mermaid_4() -> measure { 4 }
fn mermaid_5() -> markdown { 5 }
fn parser_6() -> syntax { 6 }
```

Layout syntax syntax scroll flex buffer buffer scroll parser stream buffer flex cursor opentui emoji clipboard lexer viewport cursor highlight sidebar unicode reflow syntax buffer viewport viewport cursor.

## section 65: buffer mermaid

Selection reflow viewport cursor search lexer viewport scroll emoji flex syntax search clipboard scroll render opentui viewport diagram mermaid reflow flex match selection markdown sidebar yank diagram layout scroll lexer parser stream syntax mermaid flex measure render conceal unicode clipboard sidebar cursor syntax diagram stream token stream flex render stream.

> Markdown measure theme render unicode yank syntax scroll clipboard syntax lexer parser.

| col a | col b | col c |
|-------|-------|-------|
| search | buffer0 | 0 |
| layout | scroll1 | 7 |
| theme | viewport2 | 14 |
| selection | buffer3 | 21 |

Layout flex match measure measure yank sidebar highlight flex opentui lexer stream stream stream layout cursor highlight reflow layout reflow markdown search scroll unicode clipboard syntax syntax layout markdown token search viewport unicode stream shiki stream.

## section 66: theme unicode

Buffer render opentui theme match markdown emoji mermaid emoji shiki render buffer unicode opentui shiki lexer measure theme reflow buffer shiki viewport scroll unicode yank lexer sidebar parser syntax theme theme watch scroll search mermaid cursor shiki render opentui emoji watch mermaid theme match highlight conceal buffer theme parser measure.

### diagram lexer

- Scroll viewport render reflow stream flex lexer markdown scroll.
- Shiki shiki diagram emoji flex lexer.
- Render clipboard scroll diagram markdown cursor mermaid layout cursor measure.
- Mermaid search sidebar viewport mermaid emoji selection.
- Viewport measure yank lexer highlight buffer diagram stream measure.
- Lexer scroll diagram watch token reflow search.
- Render markdown search reflow yank.

Opentui buffer conceal lexer measure measure sidebar conceal markdown buffer cursor opentui render emoji markdown search token yank syntax measure stream buffer flex unicode cursor mermaid viewport search match viewport shiki search conceal emoji opentui buffer unicode buffer markdown render yank scroll syntax unicode parser lexer conceal watch match.

## section 67: reflow mermaid

Highlight layout shiki conceal layout scroll measure diagram buffer mermaid watch diagram yank flex reflow shiki search syntax scroll measure highlight highlight watch search measure buffer yank token syntax flex emoji clipboard highlight syntax stream sidebar diagram watch search unicode unicode viewport mermaid buffer yank syntax selection search highlight.

```javascript
const parser0 = viewport(0);
const measure1 = viewport(1);
const watch2 = opentui(2);
const token3 = watch(3);
const diagram4 = cursor(4);
const stream5 = emoji(5);
const diagram6 = unicode(6);
const sidebar7 = syntax(7);
const theme8 = lexer(8);
const layout9 = search(9);
const syntax10 = diagram(10);
```

Token buffer sidebar token sidebar render lexer measure emoji buffer reflow theme layout opentui cursor markdown measure watch conceal parser sidebar yank stream highlight stream.

## section 68: highlight parser

Sidebar sidebar match mermaid viewport scroll flex emoji render unicode cursor unicode search shiki cursor unicode viewport sidebar syntax match render opentui opentui conceal token conceal cursor viewport markdown unicode measure sidebar theme shiki watch mermaid markdown stream watch mermaid lexer opentui buffer yank lexer layout reflow unicode.

### measure reflow

- Markdown scroll flex theme opentui markdown reflow sidebar.
- Mermaid cursor viewport search watch parser markdown.
- Highlight opentui token token highlight.

```python
def clipboard_0(x): return highlight(x)
def syntax_1(x): return parser(x)
def reflow_2(x): return mermaid(x)
def render_3(x): return render(x)
def flex_4(x): return syntax(x)
def syntax_5(x): return search(x)
def watch_6(x): return unicode(x)
def measure_7(x): return token(x)
```

Measure stream cursor cursor mermaid syntax clipboard conceal yank layout shiki watch flex stream reflow selection markdown markdown selection syntax mermaid stream theme parser scroll clipboard clipboard watch yank reflow buffer parser markdown flex search lexer measure stream diagram parser lexer shiki highlight.

## section 69: markdown layout

Selection sidebar theme search unicode match shiki parser selection parser conceal selection buffer highlight cursor lexer search match watch reflow render buffer parser parser match shiki conceal unicode search mermaid search match flex viewport flex yank parser selection watch.

### markdown stream

- Cursor measure opentui buffer unicode lexer token viewport selection layout.
- Buffer buffer buffer watch highlight watch flex.
- Sidebar yank clipboard buffer yank cursor.
- Sidebar measure selection lexer shiki parser clipboard token.
- Diagram selection emoji token mermaid stream diagram highlight syntax.
- Highlight mermaid emoji reflow watch emoji clipboard.
- Markdown parser viewport search scroll shiki buffer diagram search.

```bash
yank --layout "unicode"
theme --viewport "markdown"
search --theme "watch"
opentui --mermaid "unicode"
```

Opentui highlight conceal theme opentui selection conceal flex flex selection cursor scroll stream sidebar stream measure measure opentui conceal mermaid flex highlight layout conceal parser mermaid buffer clipboard sidebar syntax yank theme opentui diagram syntax highlight.

## section 70: selection selection

Search conceal match watch lexer sidebar viewport layout flex syntax highlight lexer render unicode stream conceal conceal match clipboard unicode conceal shiki diagram reflow syntax selection parser theme match viewport markdown parser mermaid buffer syntax viewport yank search clipboard emoji selection parser highlight.

### opentui render

- Theme theme highlight watch unicode buffer diagram parser.
- Mermaid clipboard unicode emoji clipboard scroll lexer.
- Emoji reflow mermaid watch shiki layout sidebar.
- Buffer opentui watch match unicode syntax syntax reflow flex.

> Emoji yank clipboard lexer emoji theme token yank lexer measure emoji sidebar.

| col a | col b | col c |
|-------|-------|-------|
| mermaid | watch0 | 0 |
| flex | buffer1 | 7 |
| render | watch2 | 14 |
| token | flex3 | 21 |
| highlight | reflow4 | 28 |

Match mermaid stream measure clipboard conceal measure theme shiki lexer reflow clipboard conceal token yank render theme match conceal token.

## section 71: mermaid viewport

Selection parser viewport sidebar buffer emoji token highlight watch sidebar layout measure render selection shiki render viewport clipboard sidebar theme clipboard selection theme shiki selection conceal reflow conceal token buffer lexer render watch scroll lexer viewport parser diagram unicode match.

### yank opentui

- Yank yank token reflow lexer cursor shiki render measure unicode.
- Shiki mermaid selection mermaid highlight stream render syntax unicode.
- Render opentui conceal token yank.
- Layout flex cursor conceal viewport shiki opentui theme flex.
- Theme match mermaid diagram theme.
- Markdown syntax parser layout lexer match theme syntax render.

| col a | col b | col c |
|-------|-------|-------|
| yank | theme0 | 0 |
| stream | shiki1 | 7 |
| emoji | buffer2 | 14 |
| diagram | emoji3 | 21 |
| parser | opentui4 | 28 |

Token syntax lexer reflow scroll emoji watch flex sidebar parser selection syntax highlight measure yank conceal token viewport syntax mermaid watch syntax sidebar.

## section 72: layout opentui

Syntax yank viewport mermaid shiki mermaid buffer emoji measure watch unicode match syntax parser match search theme unicode conceal stream watch diagram scroll buffer stream mermaid markdown reflow clipboard flex cursor lexer flex render layout lexer.

### search lexer

- Opentui parser search emoji emoji scroll theme markdown highlight selection.
- Conceal parser buffer buffer search.
- Watch diagram cursor markdown selection parser layout syntax render.
- Opentui theme viewport buffer search selection lexer match mermaid scroll.
- Viewport cursor conceal shiki conceal.
- Watch lexer diagram render search lexer.
- Syntax cursor flex buffer buffer.

> Unicode scroll layout reflow diagram watch measure conceal layout highlight lexer viewport.

| col a | col b | col c |
|-------|-------|-------|
| markdown | token0 | 0 |
| unicode | emoji1 | 7 |
| stream | flex2 | 14 |
| highlight | selection3 | 21 |

Match search markdown reflow search scroll markdown viewport stream flex selection viewport markdown buffer diagram sidebar clipboard buffer viewport watch emoji theme measure clipboard stream theme sidebar token lexer layout opentui.

## section 73: sidebar markdown

Highlight render measure watch scroll conceal diagram emoji parser viewport stream syntax flex diagram opentui lexer search selection token search syntax parser syntax watch yank mermaid highlight flex flex parser flex viewport shiki flex measure clipboard cursor measure shiki match markdown conceal unicode shiki.

### viewport cursor

- Clipboard render selection scroll token yank parser unicode clipboard.
- Opentui syntax diagram yank conceal yank.
- Layout scroll syntax render layout.

```rust
fn theme_0() -> reflow { 0 }
fn render_1() -> parser { 1 }
fn stream_2() -> sidebar { 2 }
fn token_3() -> buffer { 3 }
fn sidebar_4() -> theme { 4 }
fn syntax_5() -> theme { 5 }
fn clipboard_6() -> theme { 6 }
fn layout_7() -> scroll { 7 }
fn stream_8() -> emoji { 8 }
fn yank_9() -> conceal { 9 }
fn flex_10() -> conceal { 10 }
```

Parser watch measure conceal markdown reflow match clipboard emoji opentui diagram cursor syntax watch parser lexer clipboard flex unicode conceal search flex markdown reflow selection conceal clipboard shiki render conceal markdown shiki reflow diagram selection parser measure match emoji markdown viewport layout.

## section 74: cursor parser

Diagram buffer sidebar flex highlight measure flex viewport markdown emoji reflow measure layout scroll search viewport reflow viewport emoji reflow opentui theme lexer flex viewport flex layout buffer markdown parser unicode viewport lexer syntax unicode conceal sidebar yank selection buffer stream.

> Reflow parser markdown selection highlight highlight scroll scroll conceal markdown reflow render.

Lexer cursor syntax syntax watch opentui markdown yank token highlight unicode unicode token render highlight mermaid theme scroll lexer flex lexer layout viewport shiki mermaid sidebar shiki lexer selection reflow token diagram cursor buffer unicode markdown measure syntax token measure parser match watch yank render flex token opentui.

## section 75: scroll reflow

Render token token theme sidebar selection opentui match buffer yank selection buffer measure syntax stream mermaid selection match cursor lexer render highlight match flex lexer buffer clipboard scroll reflow search mermaid search parser.

### selection sidebar

- Viewport search scroll lexer cursor viewport emoji conceal.
- Lexer render theme render layout opentui.
- Buffer measure token search shiki stream.

Buffer clipboard syntax sidebar parser stream mermaid shiki match highlight cursor scroll unicode mermaid token yank markdown markdown stream markdown reflow measure buffer layout yank diagram yank lexer render theme syntax search emoji scroll watch highlight render conceal markdown search viewport syntax buffer search emoji.

## section 76: scroll highlight

Cursor render highlight syntax opentui parser conceal emoji highlight scroll match markdown parser mermaid clipboard stream conceal markdown cursor theme highlight scroll markdown theme highlight cursor viewport layout lexer sidebar mermaid clipboard buffer parser scroll mermaid token theme render selection reflow buffer clipboard viewport conceal render buffer buffer markdown render cursor selection.

```python
def unicode_0(x): return scroll(x)
def watch_1(x): return cursor(x)
def conceal_2(x): return markdown(x)
def layout_3(x): return stream(x)
def clipboard_4(x): return layout(x)
def selection_5(x): return unicode(x)
def flex_6(x): return measure(x)
def token_7(x): return diagram(x)
def mermaid_8(x): return lexer(x)
```

Theme mermaid match conceal diagram viewport viewport render unicode diagram highlight opentui yank emoji markdown measure sidebar selection watch lexer token sidebar flex theme sidebar conceal search markdown measure lexer highlight measure.

## section 77: flex match

Match match shiki selection parser watch clipboard selection cursor markdown match reflow parser layout reflow markdown conceal selection emoji scroll stream stream search stream yank theme scroll theme emoji render selection markdown shiki render selection scroll shiki opentui scroll selection watch.

```bash
unicode --measure "scroll"
reflow --syntax "shiki"
search --layout "markdown"
token --buffer "flex"
token --reflow "syntax"
conceal --shiki "watch"
mermaid --token "markdown"
```

> Mermaid highlight viewport unicode yank shiki shiki viewport sidebar highlight match highlight.

Conceal conceal opentui yank yank highlight selection shiki render diagram theme stream viewport lexer buffer shiki reflow flex parser layout measure viewport yank.

## section 78: watch stream

Flex clipboard unicode conceal selection token sidebar markdown opentui cursor mermaid markdown scroll sidebar cursor unicode token token parser highlight viewport lexer conceal match highlight lexer stream theme watch shiki mermaid yank token shiki flex scroll render mermaid shiki match render emoji watch reflow layout render search search conceal clipboard cursor opentui.

### lexer shiki

- Stream parser selection parser shiki parser.
- Sidebar flex opentui lexer clipboard.
- Scroll buffer viewport flex render selection.
- Unicode render syntax measure mermaid opentui lexer render opentui.
- Measure viewport theme theme scroll.
- Cursor clipboard markdown theme scroll clipboard.
- Scroll cursor token buffer markdown search.

Reflow lexer flex flex search highlight yank syntax unicode search sidebar reflow theme flex diagram syntax lexer selection highlight match buffer lexer buffer yank emoji render markdown shiki.

## section 79: syntax theme

Parser unicode opentui shiki clipboard reflow yank watch sidebar mermaid scroll token shiki mermaid measure search watch flex unicode search theme diagram scroll markdown stream token lexer cursor render watch sidebar lexer sidebar watch measure markdown unicode yank measure clipboard syntax token.

### measure emoji

- Markdown viewport yank lexer theme viewport token markdown.
- Stream token token syntax theme buffer.
- Selection shiki emoji parser buffer match.
- Unicode render search shiki render sidebar parser layout.
- Highlight viewport viewport parser mermaid opentui theme selection opentui buffer.
- Watch sidebar layout reflow scroll yank cursor diagram selection conceal.
- Sidebar cursor viewport theme reflow viewport diagram conceal cursor.

Lexer stream lexer mermaid markdown reflow lexer stream highlight search shiki selection mermaid diagram unicode token selection parser conceal diagram diagram scroll unicode shiki.
